<%- include("../includes/head.ejs") %> <%- include("../includes/navigation.ejs")
%>
<main>
  <div class="title_with_bottom_border">Order #<%= orderId %> Overview</div>
  <div class="flex-box gap-half-rem">
    <% if(orderProducts) { %> <% orderProducts.forEach(function (order_product){
    %>
    <div class="card cart-product-item flex-box">
      <h2><%= order_product?.product_id?.title %></h2>
      <p><%= order_product?.quantity %></p>
      <button class="btn">
        ₹ <%= order_product?.product_id?.price*order_product?.quantity %>
      </button>
    </div>
    <% }) %>
  </div>
  <% }else{ %>
  <p class="empty_cart_msg">No Ordered Products !</p>
  <% } %>
</main>
<%- include("../includes/end.ejs") %>
