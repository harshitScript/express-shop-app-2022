<%- include("../includes/head.ejs") %> <%- include("../includes/navigation.ejs")
%>
<main>
  <% if (products.length > 0) { %>
  <div class="flex-box">
    <% products.forEach(function(singleProduct){ %>
    <article class="card product-item">
      <header>
        <h1 class="product__title"><%= singleProduct.title %></h1>
        <div class="card__image">
          <img
            src="/<%= singleProduct.imageURL %>"
            alt="image"
            width="200px"
            height="200px"
          />
        </div>
        <div class="card__content">
          <span class="product__price">₹<%= singleProduct.price %></span>
          <p class="product__description"><%= singleProduct.description %></p>
        </div>
        <div class="card__actions">
          <%-
          include("../includes/add-to-cart.ejs",{singleProduct:singleProduct})
          %>
          <a href="/shop/product-details/<%= singleProduct._id %>" class="btn"
            >See Details</a
          >
        </div>
        <p class="product-id">#<%= singleProduct._id %></p>
      </header>
    </article>
    <% }); %> <%- include("../includes/logout-btn.ejs") %> <%-
    include("../includes/pagination.ejs",{numberOfPages,type :"shop"}) %>
  </div>
  <% } else { %>
  <p>No products</p>
  <% } %>
</main>
<%- include("../includes/end.ejs") %>
